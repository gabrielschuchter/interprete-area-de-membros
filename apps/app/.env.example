# Runtime database URL: Supabase transaction pooler (serverless-safe).
DATABASE_URL="postgresql://postgres.wkclodjbrynerfgufmyb:replace-with-password@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# Private Supabase Storage for server-side signed asset URLs. Never expose the secret key to the browser.
SUPABASE_URL="https://wkclodjbrynerfgufmyb.supabase.co"
SUPABASE_STORAGE_BUCKET="learning-assets"
# Public Realtime connection (the anon key is safe to expose; never use the service role here).
# SUPABASE_ANON_KEY="ey..."
# SUPABASE_JWT_SECRET="..." # Supabase project JWT secret, server-only
# SUPABASE_SECRET_KEY="sb_secret_..."
# Dedicated high-entropy secret used only to sign short-lived HLS playback capabilities.
# Generate independently from Supabase/Clerk keys and keep it server-only.
# LEARNING_ASSET_TOKEN_SECRET="..."
# Optional override when the bundled Supabase CA must be replaced.
# DATABASE_CA_CERT_PATH="C:\\path\\to\\prod-ca-2021.crt"

# Clerk authentication
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/"
# Optional proxy override for the Clerk Production frontend API.
# NEXT_PUBLIC_CLERK_PROXY_URL="/__clerk"

# Core app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional protection and observability
ARCJET_KEY=""
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
BETTERSTACK_API_KEY=""
BETTERSTACK_URL=""

# Optional analytics integrations; no provider is configured by default.
NEXT_PUBLIC_GA_MEASUREMENT_ID=""
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST=""
