# Interprete design system

Storybook is the local catalog for the shared design system used by the Interprete member area.

```powershell
bun run dev
```

The app runs Storybook on port `6006`. Product flows and domain behavior belong in `apps/app`, not in these component stories.
